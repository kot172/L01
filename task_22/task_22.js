

document.write(document.write('1'))

// Функция вызовется 2 раза

// Это происходит из-за того, что функция выведет значение '1' на страницу, а после 
// вернет undefined, после этого она вызовется с этим же аргументом и выведет undefined